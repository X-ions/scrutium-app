@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Content monitoring" />
<div class="mb-6 flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Content monitoring</h2><p class="mt-1 text-sm text-gray-500">Track reach, engagement, and integrity from synced posts.</p></div><div class="flex gap-3 text-sm"><span class="rounded-lg bg-brand-50 px-3 py-2 text-brand-700 dark:bg-brand-500/15 dark:text-brand-400">{{ number_format($totalReach) }} reach</span><span class="rounded-lg bg-error-50 px-3 py-2 text-error-700 dark:bg-error-500/15 dark:text-error-400">{{ $flaggedCount }} flagged</span></div></div>
<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><form method="GET" class="mb-5 flex flex-wrap gap-3"><select name="platform" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All platforms</option>@foreach($platforms as $key => $label)<option value="{{ $key }}" @selected(request('platform') === $key)>{{ $label }}</option>@endforeach</select><label class="flex items-center gap-2 text-sm text-gray-500"><input type="checkbox" name="flagged" value="1" @checked(request('flagged')) class="rounded text-brand-500"> Integrity flags only</label><button class="rounded-lg bg-gray-100 px-4 text-sm dark:bg-white/5">Filter</button></form><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-gray-500 dark:bg-white/[0.03]"><tr><th class="px-3 py-3 text-start">Post</th><th class="px-3 py-3 text-start">Campaign</th><th class="px-3 py-3 text-start">Reach</th><th class="px-3 py-3 text-start">Engagement</th><th class="px-3 py-3 text-start">Integrity</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@forelse($posts as $post)<tr><td class="px-3 py-4"><a href="{{ $post->url }}" target="_blank" class="font-medium text-brand-600">{{ $post->platform->label() }} post</a><p class="max-w-xs truncate text-xs text-gray-400">{{ $post->caption }}</p></td><td class="px-3 py-4 text-gray-500">{{ $post->campaign?->name ?: 'Unassigned' }}</td><td class="px-3 py-4 text-gray-500">{{ number_format($post->reach) }}</td><td class="px-3 py-4 text-gray-500">{{ number_format($post->totalEngagements()) }}</td><td class="px-3 py-4"><span class="rounded-full px-2.5 py-1 text-xs {{ $post->isFlagged() ? 'bg-error-50 text-error-700 dark:bg-error-500/15 dark:text-error-400' : 'bg-success-50 text-success-700 dark:bg-success-500/15 dark:text-success-400' }}">{{ $post->isFlagged() ? 'Review' : 'Healthy' }}</span></td></tr>@empty<tr><td colspan="5" class="px-3 py-10 text-center text-gray-500">No content synced yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $posts->links() }}</div></div>
@endsection