@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Scoring" />
<div class="mb-6 flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Creator scoring</h2><p class="mt-1 text-sm text-gray-500">Tune weighted metrics and recalculate from workspace evidence.</p></div><form method="POST" action="{{ route('scoring.recalculate') }}">@csrf<button class="rounded-lg bg-brand-500 px-4 py-2.5 text-sm font-medium text-white">Recalculate scores</button></form></div>
<div class="grid gap-6 xl:grid-cols-3"><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">{{ $config->name }}</h3><p class="mt-1 text-sm text-gray-500">Weights must total 1.0. Current total: {{ number_format($config->weightTotal(), 2) }}.</p><form method="POST" action="{{ route('scoring.config', $config) }}" class="mt-5 space-y-3">@csrf @method('PATCH')@foreach($config->weightMap() as $metric => $weight)<div><div class="mb-1 flex justify-between text-xs text-gray-500"><span>{{ str_replace('_', ' ', $metric) }}</span><span>{{ number_format($weight, 2) }}</span></div><input name="{{ $metric }}" type="number" step="0.01" min="0" max="1" value="{{ old($metric, $weight) }}" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"></div>@endforeach<button class="w-full rounded-lg bg-brand-500 px-4 py-2.5 text-sm text-white">Save weights</button></form></div><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03] xl:col-span-2"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Latest scores</h3><div class="mt-4 overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-gray-500 dark:bg-white/[0.03]"><tr><th class="px-3 py-3 text-start">Creator</th><th class="px-3 py-3 text-start">Score</th><th class="px-3 py-3 text-start">Band</th><th class="px-3 py-3 text-start">Computed</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@forelse($scores as $score)<tr><td class="px-3 py-3 font-medium text-gray-800 dark:text-white/90">{{ $score->influencer->displayName() }}</td><td class="px-3 py-3 text-lg font-semibold text-brand-600">{{ number_format((float) $score->score, 1) }}</td><td class="px-3 py-3 text-gray-500">{{ $score->band() }}</td><td class="px-3 py-3 text-gray-500">{{ $score->computed_at->format('M j, Y') }}</td></tr>@empty<tr><td colspan="4" class="px-3 py-10 text-center text-gray-500">Recalculate to generate scores.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $scores->links() }}</div></div></div>
@endsection