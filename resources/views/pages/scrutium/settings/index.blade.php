@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Settings" />
<div class="mb-6"><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Workspace settings</h2><p class="mt-1 text-sm text-gray-500">Manage workspace details, team roles, and notification preferences.</p></div>
<div class="grid gap-6 xl:grid-cols-2"><div class="space-y-6"><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Workspace</h3><form method="POST" action="{{ route('settings.workspace') }}" class="mt-5 space-y-3">@csrf @method('PATCH')<input name="name" value="{{ $tenant->name }}" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="timezone" value="{{ $tenant->timezone }}" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><select name="currency" class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white">@foreach(['USD','EUR','GBP','AED','SAR'] as $currency)<option value="{{ $currency }}" @selected($tenant->currency === $currency)>{{ $currency }}</option>@endforeach</select><button class="rounded-lg bg-brand-500 px-4 py-2.5 text-sm text-white">Save workspace</button></form></div><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Notification subscriptions</h3><div class="mt-4 space-y-2">@foreach($subscriptions as $subscription)<div class="flex justify-between rounded-lg bg-gray-50 p-3 text-sm dark:bg-white/[0.03]"><span>{{ $subscription->type ?: 'All alerts' }} · {{ $subscription->channel }}</span><span>{{ $subscription->is_active ? 'Active' : 'Paused' }}</span></div>@endforeach</div><form method="POST" action="{{ route('settings.subscriptions.store') }}" class="mt-4 flex gap-2">@csrf<select name="channel" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="in_app">In-app</option><option value="email">Email</option></select><button class="rounded-lg bg-brand-500 px-4 text-sm text-white">Add</button></form></div></div><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Team members</h3><div class="mt-4 space-y-3">@foreach($members as $member)<div class="flex items-center justify-between gap-3 border-b border-gray-100 pb-3 dark:border-gray-800"><div><p class="font-medium text-gray-800 dark:text-white/90">{{ $member->name }}</p><p class="text-xs text-gray-400">{{ $member->email }}</p></div><form method="POST" action="{{ route('settings.members.update', $member) }}" class="flex gap-2">@csrf @method('PATCH')<select name="role" class="rounded-lg border border-gray-300 px-2 py-1 text-xs dark:border-gray-700 dark:bg-gray-900 dark:text-white">@foreach($roles as $key => $label)<option value="{{ $key }}" @selected($member->role->value === $key)>{{ $label }}</option>@endforeach</select><button class="rounded-lg bg-gray-100 px-2 py-1 text-xs dark:bg-white/5">Save</button></form></div>@endforeach</div><h4 class="mt-6 text-sm font-semibold text-gray-800 dark:text-white/90">Invite member</h4><form method="POST" action="{{ route('settings.members.store') }}" class="mt-3 space-y-3">@csrf<input name="name" placeholder="Name" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="email" type="email" placeholder="Email" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="temporary_password" type="password" minlength="12" placeholder="Temporary password" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><select name="role" class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white">@foreach($roles as $key => $label)<option value="{{ $key }}">{{ $label }}</option>@endforeach</select><button class="w-full rounded-lg bg-brand-500 px-4 py-2.5 text-sm text-white">Add team member</button></form></div></div>
@endsection