@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Influencers" />
<div class="mb-6 flex items-center justify-between gap-3"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Creator intelligence</h2><p class="mt-1 text-sm text-gray-500">Source, vet, and staff creators with confidence.</p></div><button onclick="document.getElementById('creator-form').classList.toggle('hidden')" class="rounded-lg bg-brand-500 px-4 py-2.5 text-sm font-medium text-white">Add creator</button></div>
<form id="creator-form" method="POST" action="{{ route('influencers.store') }}" class="mb-6 hidden rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]">@csrf<div class="grid gap-4 md:grid-cols-4"><input name="handle" placeholder="Handle" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><select name="platform" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white">@foreach($platforms as $key => $label)<option value="{{ $key }}">{{ $label }}</option>@endforeach</select><input name="full_name" placeholder="Full name" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="followers" type="number" min="0" placeholder="Followers" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="engagement_rate" type="number" min="0" max="100" step="0.01" placeholder="Engagement %" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><button class="rounded-lg bg-brand-500 px-4 text-sm text-white">Save creator</button></div></form>
<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><form method="GET" class="mb-5 grid gap-3 md:grid-cols-4"><input name="search" value="{{ request('search') }}" placeholder="Search creators" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><select name="platform" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All platforms</option>@foreach($platforms as $key => $label)<option value="{{ $key }}" @selected(request('platform') === $key)>{{ $label }}</option>@endforeach</select><select name="status" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All vetting stages</option>@foreach($statuses as $key => $label)<option value="{{ $key }}" @selected(request('status') === $key)>{{ $label }}</option>@endforeach</select><button class="rounded-lg bg-gray-100 text-sm dark:bg-white/5">Filter</button></form><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-gray-500 dark:bg-white/[0.03]"><tr><th class="px-3 py-3 text-start">Creator</th><th class="px-3 py-3 text-start">Audience</th><th class="px-3 py-3 text-start">Pulse</th><th class="px-3 py-3 text-start">Vetting</th><th class="px-3 py-3 text-start">Campaigns</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@forelse($creators as $creator)<tr><td class="px-3 py-4"><p class="font-medium text-gray-800 dark:text-white/90">{{ $creator->displayName() }}</p><p class="text-xs text-gray-400">{{ $creator->platform->label() }} · {{ $creator->handle }}</p></td><td class="px-3 py-4 text-gray-500">{{ $creator->formattedFollowers() }} · {{ $creator->engagement_rate }}%</td><td class="px-3 py-4 font-semibold text-gray-800 dark:text-white/90">{{ $creator->pulse_score ?? '—' }}</td><td class="px-3 py-4"><form method="POST" action="{{ route('influencers.vetting', $creator) }}" class="flex gap-2">@csrf @method('PATCH')<select name="vetting_status" onchange="this.form.submit()" class="rounded-full border-0 px-2.5 py-1 text-xs {{ $creator->vetting_status->badgeColor() }}">@foreach($statuses as $key => $label)<option value="{{ $key }}" @selected($creator->vetting_status->value === $key)>{{ $label }}</option>@endforeach</select></form></td><td class="px-3 py-4 text-gray-500">{{ $creator->campaigns_count }}</td></tr>@empty<tr><td colspan="5" class="px-3 py-10 text-center text-gray-500">No creators found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $creators->links() }}</div></div>
@endsection