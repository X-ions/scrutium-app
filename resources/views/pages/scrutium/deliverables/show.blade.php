@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb :pageTitle="$deliverable->title" />
<div class="mb-6 flex items-center justify-between gap-3"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">{{ $deliverable->title }}</h2><p class="mt-1 text-sm text-gray-500">{{ $deliverable->campaign->name }} · {{ $deliverable->influencer->displayName() }}</p></div><a href="{{ route('deliverables') }}" class="text-sm font-medium text-brand-600">Back</a></div>
<div class="grid gap-6 xl:grid-cols-3"><div class="space-y-6 xl:col-span-2"><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><div class="flex items-center justify-between"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Verification</h3><span class="rounded-full px-2.5 py-1 text-xs {{ $deliverable->status->badgeColor() }}">{{ $deliverable->status->label() }}</span></div><div class="mt-5 grid gap-4 sm:grid-cols-3"><div><p class="text-xs text-gray-400">Type</p><p class="mt-1 text-sm">{{ $deliverable->type->label() }}</p></div><div><p class="text-xs text-gray-400">Units</p><p class="mt-1 text-sm">{{ $deliverable->delivered_units }} / {{ $deliverable->contracted_units }}</p></div><div><p class="text-xs text-gray-400">Due</p><p class="mt-1 text-sm">{{ $deliverable->due_at?->format('M j, Y') ?: '—' }}</p></div></div>@if($evidence['status'] !== 'none')<div class="mt-5 rounded-xl border border-gray-200 bg-gray-50 p-4 dark:border-gray-800 dark:bg-white/[0.03]"><p class="text-xs font-medium uppercase tracking-wider text-gray-400 dark:text-gray-500">Evidence</p>@if($evidence['url'])<a href="{{ $evidence['url'] }}" target="_blank" rel="noopener noreferrer" class="mt-2 inline-flex items-center gap-1.5 text-sm font-medium text-brand-600 transition hover:text-brand-700 dark:text-brand-400 dark:hover:text-brand-300"><span>{{ $evidence['status'] === 'external' ? 'Open the evidence link' : 'Open '.$evidence['name'] }}</span><span aria-hidden="true" class="rtl:rotate-180">&rarr;</span></a>@else<p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Evidence was recorded, but the stored file is no longer available. Ask the creator to resubmit.</p>@endif</div>@endif @if($deliverable->rejection_reason)<div class="mt-4 rounded-xl border border-error-500/30 bg-error-50 p-4 dark:border-error-500/30 dark:bg-error-500/10"><p class="text-xs font-medium uppercase tracking-wider text-error-600 dark:text-error-400">Rejection reason</p><p class="mt-2 text-sm text-error-700 dark:text-error-300">{{ $deliverable->rejection_reason }}</p></div>@endif @if($deliverable->status->value !== 'approved' && $deliverable->status->value !== 'rejected')<form method="POST" action="{{ route('deliverables.submit', $deliverable) }}" enctype="multipart/form-data" class="mt-6 space-y-3 border-t border-gray-100 pt-5 dark:border-gray-800">@csrf<div class="grid gap-3 sm:grid-cols-2"><input name="delivered_units" type="number" min="1" value="{{ $deliverable->delivered_units ?: 1 }}" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="evidence_url" type="url" placeholder="Evidence URL (optional)" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"></div><input name="evidence" type="file" accept=".jpg,.jpeg,.png,.pdf,.mp4" class="block w-full text-sm text-gray-500"><button class="rounded-lg bg-brand-500 px-4 py-2.5 text-sm text-white">Submit evidence</button></form>@endif @if($deliverable->status->value === 'submitted')<div class="mt-6 flex flex-wrap gap-3 border-t border-gray-100 pt-5 dark:border-gray-800"><form method="POST" action="{{ route('deliverables.approve', $deliverable) }}">@csrf<button class="rounded-lg bg-success-500 px-4 py-2.5 text-sm text-white">Approve</button></form><form method="POST" action="{{ route('deliverables.reject', $deliverable) }}" class="flex gap-2">@csrf<input name="reason" placeholder="Reason" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><button class="rounded-lg bg-error-500 px-4 text-sm text-white">Reject</button></form></div>@endif</div></div>
<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Audit trail</h3><div class="mt-4 space-y-4">@forelse($deliverable->auditEvents as $event)<div class="border-b border-gray-100 pb-3 dark:border-gray-800"><p class="text-sm font-medium text-gray-800 dark:text-white/90">{{ $event->actionLabel() }}</p><p class="mt-1 text-xs text-gray-400 dark:text-gray-500">{{ $event->created_at->format('M j, Y H:i') }} @if($event->user) · {{ $event->user->name }} @endif</p>@if($event->note)<p class="mt-1.5 text-xs text-gray-600 dark:text-gray-300">{{ $event->note }}</p>@endif</div>@empty<p class="text-sm text-gray-500">No events recorded.</p>@endforelse</div></div></div>
<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Linked content</h3>@if($deliverable->contentPost)<div class="mt-4"><a href="{{ $deliverable->contentPost->url }}" target="_blank" class="text-sm font-medium text-brand-600">Open original post</a><p class="mt-2 text-sm text-gray-500">{{ number_format($deliverable->contentPost->reach) }} reach · {{ $deliverable->contentPost->totalEngagements() }} engagements</p></div>@else<p class="mt-4 text-sm text-gray-500">No linked content post.</p>@endif</div></div>
@endsection