@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Deliverables" />
<div class="mb-6 flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Deliverable accountability</h2><p class="mt-1 text-sm text-gray-500">Submit evidence, verify delivery, and preserve the audit trail.</p></div><a href="{{ route('campaigns') }}" class="text-sm font-medium text-brand-600">Manage from campaigns</a></div>
<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><form method="GET" class="mb-5 grid gap-3 md:grid-cols-4"><select name="status" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All statuses</option>@foreach($statuses as $key => $label)<option value="{{ $key }}" @selected(request('status') === $key)>{{ $label }}</option>@endforeach</select><label class="flex items-center gap-2 text-sm text-gray-500"><input type="checkbox" name="overdue" value="1" @checked(request('overdue')) class="rounded text-brand-500"> Overdue only</label><button class="rounded-lg bg-gray-100 text-sm dark:bg-white/5">Filter</button></form><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-gray-500 dark:bg-white/[0.03]"><tr><th class="px-3 py-3 text-start">Deliverable</th><th class="px-3 py-3 text-start">Campaign / creator</th><th class="px-3 py-3 text-start">Status</th><th class="px-3 py-3 text-start">Due</th><th class="px-3 py-3 text-start">Fee</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@forelse($deliverables as $deliverable)<tr><td class="px-3 py-4"><a href="{{ route('deliverables.show', $deliverable) }}" class="font-medium text-brand-600">{{ $deliverable->title }}</a><p class="text-xs text-gray-400">{{ $deliverable->type->label() }} · {{ $deliverable->platform->label() }}</p></td><td class="px-3 py-4 text-gray-500">{{ $deliverable->campaign->name }}<br><span class="text-xs">{{ $deliverable->influencer->displayName() }}</span></td><td class="px-3 py-4"><span class="rounded-full px-2.5 py-1 text-xs {{ $deliverable->status->badgeColor() }}">{{ $deliverable->status->label() }}</span></td><td class="px-3 py-4 text-gray-500">{{ $deliverable->due_at?->format('M j, Y') ?: '—' }}</td><td class="px-3 py-4 text-gray-500">{{ $deliverable->campaign->currency }} {{ number_format((float) $deliverable->fee, 2) }}</td></tr>@empty<tr><td colspan="5" class="px-3 py-10 text-center text-gray-500">No deliverables match this view.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $deliverables->links() }}</div></div>
@endsection