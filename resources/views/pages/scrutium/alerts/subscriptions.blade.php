@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Alert subscriptions" />
<div class="mx-auto max-w-3xl"><div class="mb-6"><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Notification preferences</h2><p class="mt-1 text-sm text-gray-500">Choose which channels receive your workspace alerts.</p></div><div class="space-y-3">@forelse($subscriptions as $subscription)<div class="flex items-center justify-between rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><div><p class="font-medium text-gray-800 dark:text-white/90">{{ $subscription->type ?: 'All alert types' }}</p><p class="text-sm text-gray-500">{{ $subscription->channel === 'email' ? 'Email' : 'In-app notifications' }}</p></div><form method="POST" action="{{ route('alerts.subscriptions.update', $subscription) }}">@csrf @method('PATCH')<input type="hidden" name="is_active" value="{{ $subscription->is_active ? 0 : 1 }}"><button class="rounded-full px-3 py-1.5 text-xs font-medium {{ $subscription->is_active ? 'bg-success-50 text-success-700 dark:bg-success-500/15 dark:text-success-400' : 'bg-gray-100 text-gray-600 dark:bg-gray-800 dark:text-gray-400' }}">{{ $subscription->is_active ? 'Active' : 'Paused' }}</button></form></div>@empty<p class="rounded-2xl border border-gray-200 bg-white p-6 text-center text-gray-500 dark:border-gray-800 dark:bg-white/[0.03]">No subscriptions yet.</p>@endforelse</div></div>
@endsection