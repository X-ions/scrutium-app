@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Alerts" />
<div class="mb-6 flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Alert center</h2><p class="mt-1 text-sm text-gray-500">Triage compliance, sync, and deliverable issues.</p></div><a href="{{ route('alerts.subscriptions') }}" class="text-sm font-medium text-brand-600">Notification preferences</a></div>
<div class="mb-6 rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><form method="POST" action="{{ route('alerts.store') }}" class="grid gap-3 md:grid-cols-4">@csrf<input name="title" placeholder="Alert title" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="type" placeholder="Type" required class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><select name="severity" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white">@foreach($severities as $key => $label)<option value="{{ $key }}">{{ $label }}</option>@endforeach</select><button class="rounded-lg bg-brand-500 px-4 text-sm text-white">Create alert</button></form></div>
<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><form method="GET" class="mb-5 flex flex-wrap gap-3"><select name="status" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All statuses</option>@foreach($statuses as $key => $label)<option value="{{ $key }}" @selected(request('status') === $key)>{{ $label }}</option>@endforeach</select><select name="severity" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All severities</option>@foreach($severities as $key => $label)<option value="{{ $key }}" @selected(request('severity') === $key)>{{ $label }}</option>@endforeach</select><button class="rounded-lg bg-gray-100 px-4 text-sm dark:bg-white/5">Filter</button></form><div class="space-y-3">@forelse($alerts as $alert)<div class="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-gray-100 p-4 dark:border-gray-800"><div class="min-w-0"><div class="flex flex-wrap items-center gap-2"><span class="rounded-full px-2.5 py-1 text-xs {{ $alert->severity->badgeColor() }}">{{ $alert->severity->label() }}</span><p class="font-medium text-gray-800 dark:text-white/90">{{ $alert->title }}</p></div><p class="mt-1 text-sm text-gray-500">{{ $alert->message }}</p></div><div class="flex items-center gap-2"><span class="text-xs text-gray-400">{{ $alert->status->label() }}</span>@if($alert->status->value === 'open')<form method="POST" action="{{ route('alerts.acknowledge', $alert) }}">@csrf<button class="text-xs text-brand-600">Acknowledge</button></form>@endif @if($alert->status->value !== 'resolved')<form method="POST" action="{{ route('alerts.resolve', $alert) }}">@csrf<button class="text-xs text-success-600">Resolve</button></form>@endif</div></div>@empty<p class="py-10 text-center text-gray-500">No alerts in this view.</p>@endforelse</div><div class="mt-5">{{ $alerts->links() }}</div></div>
@endsection