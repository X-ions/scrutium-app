@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Performance" />
<div class="mb-6 flex items-center justify-between"><div><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Performance intelligence</h2><p class="mt-1 text-sm text-gray-500">Spend, reach, engagement, and cost efficiency from live workspace data.</p></div><form method="GET"><select name="campaign_id" x-on:change="$el.form.submit()" class="h-10 rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="">All campaigns</option>@foreach($campaigns as $campaign)<option value="{{ $campaign->id }}" @selected($selectedCampaign === $campaign->id)>{{ $campaign->name }}</option>@endforeach</select></form></div>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Approved spend', $metrics['spend'], 'USD'],['Reach', $metrics['reach'], ''],['Engagements', $metrics['engagements'], ''],['CPE', $metrics['cpe'] ?? '—', '']] as [$label, $value, $unit])<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><p class="text-sm text-gray-500">{{ $label }}</p><p class="mt-2 text-2xl font-bold text-gray-800 dark:text-white/90">{{ is_numeric($value) ? ($unit ? '$'.number_format((float) $value, 2) : number_format((float) $value)) : $value }}</p></div>@endforeach</div>
<div class="mt-6 rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Campaign efficiency</h3><div class="mt-4 space-y-5">@forelse($byCampaign as $row)<div><div class="mb-2 flex justify-between text-sm"><span class="font-medium text-gray-700 dark:text-gray-300">{{ $row['campaign']->name }}</span><span class="text-gray-500">{{ number_format($row['reach']) }} reach · {{ $row['share'] }}%</span></div><div class="h-2.5 overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800"><div class="h-full rounded-full bg-brand-500" style="width: {{ min(100, $row['share']) }}%"></div></div></div>@empty<p class="text-sm text-gray-500">No campaign performance data yet.</p>@endforelse</div></div>
@endsection