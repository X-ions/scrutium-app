@extends('layouts.app')

@section('content')
<x-common.page-breadcrumb pageTitle="Integrations" />
<div class="mb-6"><h2 class="text-xl font-semibold text-gray-800 dark:text-white/90">Platform integrations</h2><p class="mt-1 text-sm text-gray-500">Manage provider connections used for content sync and verification.</p></div>
<div class="grid gap-6 xl:grid-cols-3"><div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><h3 class="text-lg font-semibold text-gray-800 dark:text-white/90">Add integration</h3><form method="POST" action="{{ route('integrations.store') }}" class="mt-5 space-y-3">@csrf<select name="provider" class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><option value="instagram">Instagram</option><option value="tiktok">TikTok</option><option value="youtube">YouTube</option><option value="x">X</option><option value="linkedin">LinkedIn</option><option value="shopify">Shopify</option><option value="ga4">Google Analytics 4</option></select><input name="name" placeholder="Connection name" required class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><input name="access_token" type="password" placeholder="Access token" class="h-10 w-full rounded-lg border border-gray-300 px-3 text-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white"><label class="flex items-center gap-2 text-sm text-gray-500"><input type="checkbox" name="auto_verify" value="1" class="rounded text-brand-500"> Enable automatic verification</label><button class="w-full rounded-lg bg-brand-500 px-4 py-2.5 text-sm text-white">Save integration</button></form></div><div class="space-y-3 xl:col-span-2">@forelse($integrations as $integration)<div class="rounded-2xl border border-gray-200 bg-white p-5 dark:border-gray-800 dark:bg-white/[0.03]"><div class="flex flex-wrap items-center justify-between gap-3"><div><div class="flex items-center gap-2"><h3 class="font-semibold text-gray-800 dark:text-white/90">{{ $integration->name ?: ucfirst($integration->provider) }}</h3><span class="rounded-full px-2.5 py-1 text-xs {{ $integration->status->badgeColor() }}">{{ $integration->status->label() }}</span></div><p class="mt-1 text-sm text-gray-500">{{ $integration->last_synced_at ? 'Last synced '.$integration->last_synced_at->diffForHumans() : 'Not synced yet' }} @if($integration->last_error) · {{ $integration->last_error }} @endif</p></div><div class="flex gap-2">@if($integration->status->value !== 'connected')<form method="POST" action="{{ route('integrations.connect', $integration) }}">@csrf<button class="rounded-lg bg-brand-500 px-3 py-2 text-xs text-white">Connect</button></form>@else<form method="POST" action="{{ route('integrations.disconnect', $integration) }}">@csrf<button class="rounded-lg border border-gray-300 px-3 py-2 text-xs text-gray-700 dark:border-gray-700 dark:text-gray-300">Disconnect</button></form>@endif</div></div><p class="mt-4 text-xs text-gray-400">Credentials: {{ collect($integration->maskedCredentials())->map(fn ($value, $key) => $key.': '.$value)->join(' · ') ?: 'No credentials stored' }}</p></div>@empty<div class="rounded-2xl border border-gray-200 bg-white p-8 text-center text-gray-500 dark:border-gray-800 dark:bg-white/[0.03]">No integrations configured yet.</div>@endforelse</div></div>
@endsection